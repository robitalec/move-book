---
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
```

```{r}
knitr::kable(params$counted_ids)
knitr::kable(params$counted_time[, .SD, .SDcols = -nLocByIndividual])
```

```{r}
print(params$temp)
```

```{r}
knitr::kable(params$na)
```

```{r}
print(params$bboxes)
```

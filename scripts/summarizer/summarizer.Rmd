# Title

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)

library(patchwork)

cache <- new_cache('/media/Backup Plus/drake-cache/')
```

```{r}
knitr::kable(readd(counted_ids_KEY, cache = cache))

knitr::kable(readd(counted_time_KEY, cache = cache)[, .SD, .SDcols = -'fixRateSummary'])
```

```{r}
print(readd(temp_KEY, cache = cache))
```

```{r}
knitr::kable(readd(na_KEY, cache = cache))
```

```{r}
print(readd(bboxes_KEY, cache = cache))
```
